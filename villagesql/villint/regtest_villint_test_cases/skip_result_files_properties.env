# This test creates a .result file with trailing whitespace
# villint.sh should skip .result files entirely, so the whitespace should remain
# This is NOT a violation - it's expected behavior
NO_VIOLATION=true
