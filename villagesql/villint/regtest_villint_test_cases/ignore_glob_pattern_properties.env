# Properties for ignore_glob_pattern test case
# Files matching the glob pattern are ignored, so villint won't fix violations
# But villint should succeed (not fail) since it skips the files
# Regular file is already compliant
NO_VIOLATION=true
