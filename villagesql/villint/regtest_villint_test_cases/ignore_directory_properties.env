# Properties for ignore_directory test case
# Directory is ignored, so villint won't fix violations in any files within it
# But villint should succeed (not fail) since it skips the files
NO_VIOLATION=true
