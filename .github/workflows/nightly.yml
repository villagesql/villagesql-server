name: Nightly Build and Test

on:
  schedule:
    - cron: '0 5 * * *'  # Runs daily at midnight NYC time (5 AM UTC)
  workflow_dispatch:  # Allows manual triggering

jobs:
  test-and-tag:
    name: Nightly Build and Test
    runs-on: [self-hosted, linux, x86_64]
    permissions:
      contents: write  # Required to push tags
      actions: write   # Required to read/write caches

    outputs:
      tag: ${{ steps.tags.outputs.date_tag }}

    env:
      SOURCE_DIR: ${{ github.workspace }}/source
      BUILD_DIR: ${{ github.workspace }}/build

    steps:
    - name: Checkout code
      uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      with:
        fetch-depth: 1
        fetch-tags: true
        path: source

    - name: Setup VillageSQL build environment
      uses: ./source/.github/actions/setup-villagesql-build
      with:
        cache-key-prefix: nightly

    - name: Build VillageSQL
      working-directory: ./source
      run: ./villagesql/bld_tools/build_ci.sh
      env:
        BUILD_TYPE: debug
        CMAKE_EXTRA_FLAGS: -DCMAKE_C_COMPILER_LAUNCHER=ccache -DCMAKE_CXX_COMPILER_LAUNCHER=ccache

    - name: Show ccache statistics
      run: ccache -s

    - name: Run standard tests (unit, integration with only suite village)
      uses: ./source/.github/actions/run-tests
      with:
        artifact-name: nightly-test-logs-${{ github.run_number }}

    - name: Run big tests
      uses: ./source/.github/actions/run-tests
      with:
        run-unit-tests: false
        run-big-tests: true
        artifact-name: nightly-big-test-logs-${{ github.run_number }}

    - name: Create nightly tags
      id: tags
      if: success()
      working-directory: ./source
      run: |
        git config user.name "villagesql-nightly[bot]"
        git config user.email "villagesql-nightly[bot]@users.noreply.github.com"

        # Create dated tag
        DATE_TAG="nightly.$(date +'%Y-%m-%d')"
        git tag -a "$DATE_TAG" -m "Nightly build $(date +'%Y-%m-%d %H:%M:%S UTC')"
        git push origin "$DATE_TAG"
        echo "date_tag=$DATE_TAG" >> "$GITHUB_OUTPUT"

        # Update/create nightly.latest tag (force update if exists)
        git tag -fa "nightly.latest" -m "Latest nightly build $(date +'%Y-%m-%d %H:%M:%S UTC')"
        git push origin "nightly.latest" --force

    - name: Prepare nightly info
      if: always()
      env:
        DATE_TAG: ${{ steps.tags.outputs.date_tag }}
        REF: ${{ github.ref }}
      run: |
        jq -n \
          --arg tagName "$DATE_TAG" \
          --arg gitRev "$REF" \
          --arg sha "$(git -C "$SOURCE_DIR" rev-parse HEAD)" \
          --arg commitSubject "$(git -C "$SOURCE_DIR" log -n1 --format=%s)" \
          '$ARGS.named' > nightly-info.json

        # Pretty print for humans
        jq . nightly-info.json

    - name: Upload nightly
      if: always()
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: nightly-info
        path: nightly-info.json
        if-no-files-found: warn
        retention-days: 7

